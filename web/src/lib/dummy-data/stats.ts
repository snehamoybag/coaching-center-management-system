import type { StatsType } from "../../types/StatsType";

const stats: StatsType = {
  students: 400,
  StudentsWithDuePayment: 150,
  teachers: 30,
  batches: 10,
  fees: 10,
  payments: 200,
  attendances: 1000,
};

export default stats;
